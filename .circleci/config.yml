version: 2.1
orbs:
  node: circleci/node@4.1.0
jobs:
  build:
    executor:
      name: node/default
      tag: '14.15'
    steps:
      - checkout
      - node/install-packages
      - run: npm run lint
      - run: npm run build
      - run: npm test

workflows:
    build:
      jobs:
        - build